# Optional ML dependencies.
# For CUDA, choose the matching PyTorch wheel from https://pytorch.org/get-started/locally/
# Example (CUDA 12.1):
# --extra-index-url https://download.pytorch.org/whl/cu121
# torch==2.5.1+cu121
# If you install without the extra index, pip will install the CPU build.
torch==2.5.1
